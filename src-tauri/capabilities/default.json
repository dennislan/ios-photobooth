{
  "identifier": "default",
  "description": "Default capability for the photobooth app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "http:default",
    "dialog:default",
    "fs:default"
  ]
}
